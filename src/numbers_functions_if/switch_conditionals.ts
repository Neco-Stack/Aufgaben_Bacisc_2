const x : number = Math.floor(Math.random() * 77);
const y : number = Math.floor(Math.random() * 77);

function randomGen (x : number , y: number){
    switch randomGen {
        case (x > y) :
    }
}